#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char 'b';
    scanf("%c", &b);
    if ((A<=b)&&(b>=Z)){
        printf("Uppercase");
    }
    else if((a<=b)&&(b>=z)){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}